#include "main.h"
#include "string.h"
/**
 *  * _strcmp - a function that compares two strings.
 *   * @s1: Accepts the first pointer to be compared
 *   * @s2: Accepts the second pointer to be compared
 *    *
 *     * Return: On success retunes integer
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
