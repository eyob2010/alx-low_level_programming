#include "main.h"
#include "ctype.h"
/**
 *  *  * _islower - display 1 if lower otherwise 0 
 *parameter int c accepts characters and return 1 and 0 
 * * Main return 0
 *
 *   */
int _islower(int c)
{
if (islower(c))
{
	return (1);
}
else
{
	return (0);
}
}

