#include "main.h"
#include "ctype.h"
/**
 *  * main - check the code.
 * _islower returns 1 if it is lowercase otherwise returnes 0
 *    * Return: Always 0.
*/
int _islower(int c)
{
if (islower(c))
{
	return (1);
}
else
{
	return (0);
}
}

