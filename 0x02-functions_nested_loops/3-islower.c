#include "main.h"
#include "ctype.h"
/**
 * main - check the code.
 *  
 * _islower rccepts int c and eturns 1 if it is lowercase otherwise returnes 0
*/
int _islower(int c)
{
if (islower(c))
{
	return (1);
}
else
{
	return (0);
}
}

