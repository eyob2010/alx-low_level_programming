#include"main.h"
#include"string.h"
/**
 *  * _strdup - a function that returns a pointer to a newly allocated space
 *   * @str: A parameter that accepts the string to be duplicated
 *    *
 *     * Return: On success it returnes a pointer to the duplicate.
 */
char *_strdup(char *str)
{
	char *str1;

	if (strr == NULL)
	{
		return (NULL);
	}
	else
	{
		str1 = strdup(str1);
		return (str1);
	}
}
